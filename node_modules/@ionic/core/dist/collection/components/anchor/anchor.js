import { openURL } from '../../utils/theme';
export class Anchor {
    constructor() {
        this.goBack = false;
    }
    render() {
        return h("a", { href: this.href, onClick: (ev) => openURL(this.href, ev, this.goBack) },
            h("slot", null));
    }
    static get is() { return "ion-anchor"; }
    static get properties() { return { "goBack": { "type": Boolean, "attr": "go-back" }, "href": { "type": String, "attr": "href" } }; }
}
