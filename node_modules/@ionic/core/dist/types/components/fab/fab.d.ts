import '../../stencil.core';
export declare class Fab {
    private el;
    activated: boolean;
    /**
     * Where to align the fab horizontally in the viewport.
     * Possible values are: `"left"`, `"right"`, `"center"`, `"start"`, `"end"`.
     */
    horizontal: 'left' | 'right' | 'center' | 'start' | 'end';
    /**
     * Where to align the fab vertically in the viewport.
     * Possible values are: `"top"`, `"center"`, `"bottom"`.
     */
    vertical: 'top' | 'center' | 'bottom';
    /**
     * If true, the fab will display on the edge of the header if
     * `vertical` is `"top"`, and on the edge of the footer if
     * it is `"bottom"`. Should be used with a `fixed` slot.
     */
    edge: boolean;
    /**
     * Close an active FAB list container
     */
    close(): void;
    toggleActive: () => void;
    hostData(): {
        class: {
            [x: string]: boolean | "left" | "right" | "center" | "start" | "end" | "top" | "bottom";
            ['fab-edge']: boolean;
        };
    };
    render(): JSX.Element;
}
