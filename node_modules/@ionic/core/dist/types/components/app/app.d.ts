import '../../stencil.core';
import { Config } from '../../index';
export declare class App {
    private isDevice;
    private deviceHacks;
    el: HTMLElement;
    config: Config;
    componentWillLoad(): void;
    hostData(): {
        class: {
            [x: number]: boolean;
            'enable-hover': boolean;
        };
    };
    render(): JSX.Element[];
}
