import { EventEmitter } from '../../stencil.core';
import { Config, DomController } from '../../index';
import { RouterDirection, RouterEventDetail } from './utils/interfaces';
export declare class Router {
    private routes;
    private previousPath;
    private redirects;
    private busy;
    private init;
    private state;
    private lastState;
    private timer;
    el: HTMLElement;
    config: Config;
    dom: DomController;
    base: string;
    useHash: boolean;
    ionRouteChanged: EventEmitter<RouterEventDetail>;
    componentDidLoad(): void;
    protected onRedirectChanged(ev: CustomEvent): void;
    protected onRoutesChanged(ev: CustomEvent): void;
    protected onPopState(): Promise<boolean>;
    private historyDirection();
    navChanged(direction: RouterDirection): Promise<boolean>;
    push(url: string, direction?: RouterDirection): Promise<boolean>;
    private writeNavStateRoot(path, direction);
    private writeNavState(node, chain, direction, index?);
    private setPath(path, direction);
    private getPath();
    private emitRouteChange(path, redirectPath);
}
