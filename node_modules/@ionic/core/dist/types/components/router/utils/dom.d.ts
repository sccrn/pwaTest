import { NavOutlet, RouteChain, RouteID, RouterDirection } from './interfaces';
export declare function writeNavState(root: HTMLElement | undefined, chain: RouteChain | null, direction: RouterDirection, index: number, changed?: boolean): Promise<boolean>;
export declare function readNavState(root: HTMLElement | null): {
    ids: RouteID[];
    outlet: NavOutlet;
};
