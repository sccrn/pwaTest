import { Config } from '../..';
export declare class InputShims {
    private didLoad;
    private hideCaret;
    private scrollAssist;
    private keyboardHeight;
    private hideCaretMap;
    private scrollAssistMap;
    config: Config;
    componentDidLoad(): void;
    protected onInputDidLoad(event: any): void;
    protected onInputDidUnload(event: any): void;
    private registerInput(componentEl);
    private unregisterInput(componentEl);
}
